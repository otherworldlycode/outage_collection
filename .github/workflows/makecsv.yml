name: Build CSV from JSON history

on:
  workflow_dispatch:

jobs:
  build-csv:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # Debug: show working directory and files
      - name: Debug initial state
        run: |
          echo "Current directory:"
          pwd
          echo "Files:"
          ls -al .
          echo "Data folder:"
          ls -al data

      # ------------------------------------------------------------
      # 1. Discover township list (newline-separated)
      # ------------------------------------------------------------
      - name: Discover township list
        id: towns
        run: |
          sample=$(ls data/*.json | head -n 1)

          towns=$(jq -r '
            .regionDataSets[]
            | select(.id=="townships")
            | .regions[].id
          ' "$sample" | sort)

          {
            echo "towns<<EOF"
            echo "$towns"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      # ------------------------------------------------------------
      # 2. Create CSV header if missing
      # ------------------------------------------------------------
      - name: Ensure data.csv exists with dynamic header
        run: |
          if [ ! -f data.csv ]; then
            echo "Creating data.csv header..."

            towns="${{ steps.towns.outputs.towns }}"

            # Trim ALL whitespace and replace spaces with underscores
            towns=$(echo "$towns" \
              | sed -E 's/^[[:space:]]+//; s/[[:space:]]+$//' \
              | sed 's/ /_/g')

            # Load into array safely
            mapfile -t town_array <<< "$towns"

            header="timestamp,totalOut,totalServed,outageCount"

            for t in "${town_array[@]}"; do
              header="$header,$t"
            done

            echo "$header" > data.csv
            echo "Header created:"
            echo "$header"
          else
            echo "data.csv already exists, skipping header creation."
          fi

      # ------------------------------------------------------------
      # 3. Process JSON files and append missing rows
      # ------------------------------------------------------------
      - name: Process JSON files
        run: |
          towns="${{ steps.towns.outputs.towns }}"

          # Normalize township names
          towns=$(echo "$towns" \
            | sed -E 's/^[[:space:]]+//; s/[[:space:]]+$//' \
            | sed 's/ /_/g')

          mapfile -t town_array <<< "$towns"

          for file in data/*.json; do
            ts=$(basename "$file" .json)

            if grep -q "$ts" data.csv; then
              echo "Skipping $ts (already exists)"
              continue
            fi

            echo "Processing $ts..."

            totalOut=$(jq '[.regionDataSets[].regions[].numberOut] | add' "$file")
            totalServed=$(jq '.totalServed' "$file")
            outageCount=$(jq '.outages | length' "$file")

            row="$ts,$totalOut,$totalServed,$outageCount"

            for t in "${town_array[@]}"; do
              val=$(jq -r --arg t "$t" '
                .regionDataSets[]
                | select(.id=="townships")
                | .regions[]
                | select(.id==$t)
                | .numberOut
              ' "$file")

              if [ -z "$val" ] || [ "$val" = "null" ]; then
                val=0
              fi

              row="$row,$val"
            done

            echo "$row" >> data.csv
            echo "Added row for $ts"
          done
      - name: Debug: show data.csv status
        run: |
          ls -al .
          echo "Git status:"
          git status

      # ------------------------------------------------------------
      # 4. Commit changes safely with rebase
      # ------------------------------------------------------------
      - name: Commit CSV if changed
        run: |
          git pull --rebase origin main || true

          if git diff --quiet; then
            echo "No changes to commit."
          else
            echo "Committing updated data.csv..."
            git config user.name "github-actions"
            git config user.email "github-actions@github.com"
            git add data.csv
            git commit -m "Update data.csv with township columns"
            git push
          fi
